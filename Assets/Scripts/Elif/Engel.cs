using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;


/*
 public class Engel : MonoBehaviour
{
    private Scene _scene;
    private void Awake()
    {
        // Oyun baþladýðýnda, aktif sahneyi alýr ve _scene deðiþkenine atar.
       _scene = SceneManager.GetActiveScene();
        // Debug.Log ile aktif sahnenin adýný konsola yazdýrýr.
        Debug.Log(_scene.name);
    }
    private void OnTriggerEnter2D(Collider2D collision)
    {
        // OnTriggerEnter2D fonksiyonu, bu nesnenin bir 2D Collider'ýyla temas ettiðinde tetiklenir.
        // Diðer nesne, bu tetiklenmeyi gerçekleþtiren collider'ý temsil eder.

        // Eðer diðer nesne "Player" etiketine sahip ise (yani oyuncu ise)
        if ( other.gameObject.CompareTag("Player"))
        {
            // SceneManager.LoadScene ile þu anki sahneyi tekrar yükler.
            // Böylece oyuncu belirli bir olaya tepki verirken sahne yeniden baþlatýlabilir.
            SceneManager.LoadScene(_scene.name);

        }
    }
}
  */
